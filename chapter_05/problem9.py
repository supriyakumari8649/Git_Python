# can you change the value inside a list which is contained in set s?
s = {8, 7, 12, "Supriya",[1,2]}
# In Python, set ke andar sirf immutable (change na hone wale) items hi allowed hote hain
# List ([1,2]) mutable hoti hai (change ho sakti hai)
# Isliye aapka set hi error dega