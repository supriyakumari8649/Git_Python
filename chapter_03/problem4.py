# Replace the double space from problem 3 with single spaces.
name = "supriya is a good  student"
print(name.replace("  "," "))
print(name) # strings are immutable which means that you cannot change them by running functions on them 
